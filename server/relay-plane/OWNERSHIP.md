# Relay plane ownership

**Owner:** Connectivity maintainers.

This plane owns authenticated connection brokering and bounded transport relay when direct LAN or WAN connectivity is unavailable.

It does not authorize product actions, inspect domain payloads unnecessarily, persist business truth, or terminate end-to-end security without an approved design. Changes require abuse, isolation, quota, reconnect, privacy, and load tests.

