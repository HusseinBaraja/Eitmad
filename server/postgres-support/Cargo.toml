[package]
name = "eitmad-postgres-support"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
rust-version.workspace = true

[dependencies]
sqlx-core = { version = "=0.8.6", default-features = false, features = ["_rt-tokio", "_tls-rustls-ring-webpki", "json", "uuid"] }
sqlx-postgres = { version = "=0.8.6", default-features = false, features = ["json", "uuid"] }

[lints]
workspace = true
