# Reference marker ownership

This crate owns the product-neutral reference marker domain behavior, validation,
authorization use, audit construction, persistence orchestration, compact events,
and local-first sync projection. Rust contracts remain in `eitmad-contracts`, and
SQLite implementation remains in `eitmad-storage`.
