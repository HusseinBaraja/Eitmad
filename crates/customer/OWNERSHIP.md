# Customer ownership

This crate owns customer contact behavior, Arabic-name and phone search
normalization, scoped authorization use, redacted audit construction, persistence
orchestration, compact events, and local-first sync projection. Rust contracts
remain in `eitmad-contracts`, and SQLite implementation remains in
`eitmad-storage`.
